{
    "version": "0.2.0",
    "configurations": [
        


        {
            "name": "Debug Expo Web (Chrome)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8081",
            "webRoot": "${workspaceFolder}"
        }
    ]
}